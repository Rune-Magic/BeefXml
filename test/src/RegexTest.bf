using Regex;
using System;
using System.IO;
using System.Collections;
using System.Diagnostics;

namespace Regex.Test;

static
{
	/*[Test]
	static void TestParse()
	{
		const let regex = @"^(?<protocol>https?):\/\/(?<subdomain>\w+\.)?(?<domain>[a-zA-Z0-9-]+\.[a-zA-Z]{2,})(?<port>:\d{2,5})?(\/(?<path>[a-zA-Z0-9\/\-._~!$&'()*+,;=:@]*))?(?:\?(?<query>[a-zA-Z0-9=&]+))?(?:#(?<fragment>[a-zA-Z0-9-_]+))?$";
		let parsed = Regex.Parse(regex, scope BumpAllocator());
		//Debug.Break();
	}*/
}